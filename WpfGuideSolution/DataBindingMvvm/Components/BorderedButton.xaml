<UserControl
    d:DesignHeight="67"
    d:DesignWidth="200"
    mc:Ignorable="d"
    x:Class="DataBindingMvvm.Components.BorderedButton"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Template>
        <ControlTemplate TargetType="UserControl">
            <Button
                Background="{Binding Background, ElementName=Root, FallbackValue=Black}"
                BorderBrush="{Binding BorderBrush, ElementName=Root, FallbackValue=#43B6DB}"
                BorderThickness="{Binding BorderThickness, ElementName=Root, FallbackValue=4}"
                Content="{Binding Text, ElementName=Root, FallbackValue=Button}"
                FontSize="{Binding FontSize, ElementName=Root, FallbackValue=32}"
                Foreground="{Binding Foreground, ElementName=Root, FallbackValue=#43B6DB}"
                Height="{Binding Height, ElementName=Root}"
                HorizontalAlignment="{Binding HorizontalAlignment, ElementName=Root}"
                HorizontalContentAlignment="{Binding HorizontalContentAlignment, ElementName=Root, FallbackValue=Center}"
                IsEnabled="{Binding IsEnabled, ElementName=Root, FallbackValue=True}"
                VerticalAlignment="{Binding VerticalAlignment, ElementName=Root}"
                VerticalContentAlignment="{Binding VerticalContentAlignment, ElementName=Root, FallbackValue=Center}"
                Width="{Binding Width, ElementName=Root}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{Binding CornerRadius, ElementName=Root, FallbackValue=35}"
                            RenderTransformOrigin="0.5,0.5"
                            x:Name="ButtonBorder">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1" x:Name="ButtonScale" />
                            </Border.RenderTransform>
                            <ContentPresenter
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                x:Name="ButtonContent" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" TargetName="ButtonBorder" Value="{Binding HoverBackground, ElementName=Root, FallbackValue=#3343B6DB}" />
                                <Setter Property="BorderBrush" TargetName="ButtonBorder" Value="{Binding HoverBorderBrush, ElementName=Root, FallbackValue=#43B6DB}" />
                                <Setter Property="TextElement.Foreground" TargetName="ButtonContent" Value="{Binding HoverForeground, ElementName=Root, FallbackValue=#43B6DB}" />
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation
                                                Duration="0:0:0.1"
                                                Storyboard.TargetName="ButtonScale"
                                                Storyboard.TargetProperty="ScaleX"
                                                To="1.01" />
                                            <DoubleAnimation
                                                Duration="0:0:0.1"
                                                Storyboard.TargetName="ButtonScale"
                                                Storyboard.TargetProperty="ScaleY"
                                                To="1.01" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation
                                                Duration="0:0:0.1"
                                                Storyboard.TargetName="ButtonScale"
                                                Storyboard.TargetProperty="ScaleX"
                                                To="1" />
                                            <DoubleAnimation
                                                Duration="0:0:0.1"
                                                Storyboard.TargetName="ButtonScale"
                                                Storyboard.TargetProperty="ScaleY"
                                                To="1" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" TargetName="ButtonBorder" Value="{Binding PressedBackground, ElementName=Root, FallbackValue=#43B6DB}" />
                                <Setter Property="BorderBrush" TargetName="ButtonBorder" Value="{Binding PressedBorderBrush, ElementName=Root, FallbackValue=#43B6DB}" />
                                <Setter Property="TextElement.Foreground" TargetName="ButtonContent" Value="{Binding PressedForeground, ElementName=Root, FallbackValue=Black}" />
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation
                                                Duration="0:0:0.05"
                                                Storyboard.TargetName="ButtonScale"
                                                Storyboard.TargetProperty="ScaleX"
                                                To="0.99" />
                                            <DoubleAnimation
                                                Duration="0:0:0.05"
                                                Storyboard.TargetName="ButtonScale"
                                                Storyboard.TargetProperty="ScaleY"
                                                To="0.99" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation
                                                Duration="0:0:0.1"
                                                Storyboard.TargetName="ButtonScale"
                                                Storyboard.TargetProperty="ScaleX"
                                                To="1.01" />
                                            <DoubleAnimation
                                                Duration="0:0:0.1"
                                                Storyboard.TargetName="ButtonScale"
                                                Storyboard.TargetProperty="ScaleY"
                                                To="1.01" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" TargetName="ButtonBorder" Value="#335DE4FF" />
                                <Setter Property="BorderBrush" TargetName="ButtonBorder" Value="#335DE4FF" />
                                <Setter Property="TextElement.Foreground" TargetName="ButtonContent" Value="#A7B0B3" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </ControlTemplate>
    </UserControl.Template>

</UserControl>
